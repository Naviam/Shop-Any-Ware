@{
    ViewBag.Title = "Shop Any Ware - Overview";
}

<div class="alert">
    <a class="close" data-dismiss="alert">×</a>
    <strong>Warning!</strong> Please add money to your balance so we can process incoming orders (take a photo).
</div>
<div class="row-fluid">
    <div class="span12">
        @{Html.RenderPartial("orderTable");}
    </div>
</div>
<div class="row-fluid">
    <div class="span12">
        @{Html.RenderPartial("parcelTable");}
    </div>
</div>
<div class="row-fluid">
    <div class="span8">
        @{Html.RenderPartial("balanceTable");}
    </div>
    <div class="span4">
        @{Html.RenderPartial("storeAddress", false);}
    </div>
</div>